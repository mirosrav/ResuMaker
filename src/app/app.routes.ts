import { Routes } from '@angular/router';
import { ResumeComponent } from './components/resume/resume/resume.component';
import { ResumakerhomeComponent } from './components/resumakerHome/resumakerhome/resumakerhome.component';

export const routes: Routes = [
    {
        path:'',
        component: ResumakerhomeComponent,
    },
    

];
